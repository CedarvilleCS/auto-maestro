[project]
name = "mcp"
version = "0.1.0"
description = "MCP bridge server for AutoMaestro"
requires-python = ">=3.12"
dependencies = [
  "fastapi[standard]",
  "uvicorn",
  "docker",
  "requests",
]


