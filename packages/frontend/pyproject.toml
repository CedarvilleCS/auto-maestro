[project]
name = "frontend"
version = "0.1.0"
description = "Contains the UI for visualizing the chat and orchestration."
requires-python = ">=3.12"
dependencies = [
    "aiohttp==3.12.15",
    "nicegui==3.2.0",
    "pydantic-settings==2.12.0",
]
